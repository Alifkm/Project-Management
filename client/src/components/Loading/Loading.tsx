
const Loading = () => {
  return (
    <div className="fixed inset-0 h-2 w-2 flex items-center justify-center bg-black bg-opacity-30 backdrop-blur-sm z-50">
      animated spinner
    </div>
  );
}

export default Loading;